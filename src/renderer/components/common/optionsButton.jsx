import React from 'react'
import Button from '@/components/button'
import { DOTS_HORIZONTAL as iconOptions } from '@/constants/icons'

const OptionsButton = ({}) => (
  <Button size="large" type="table-action" icon={iconOptions} />
)

export default OptionsButton
